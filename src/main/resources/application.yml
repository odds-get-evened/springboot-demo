spring:
  application:
    name: mentor-demo
  datasource:
    url: jdbc:mariadb://localhost:3306/spring_practice
    username: springuser
    password: springuser
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
